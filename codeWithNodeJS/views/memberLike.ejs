<!DOCTYPE html>
<html lang="zh-Hant">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

  <link rel="stylesheet" href="/temp.css">
  <title>會員專區(一般)</title>
</head>

<body>
  <nav>我的導覽列</nav>
  <div class="row">
    <aside class="col-3">
      <h1>會員專區</h1>
      <ul>
        <li><a href="/member/index/<%= uid %>">會員資料</a></li>
        <li><a href="/member/orderList/<%= uid %>">我的訂單</a></li>
        <li><a href="/member/like/<%= uid %>">按讚攤位</a></li>
        <li><a href="/member/chat/<%= uid %>">聊天室</a></li>
      </ul>
    </aside>
    <!-- http://localhost:3200/member/like/1 -->
    <main class="col-9">
      <ul>
        <% likedList.forEach(brand => { %>
        <li>
          商家名稱：<%= brand.brand_name %> <br />
          商家LOGO：
          <% if (brand.logo_img) { %>
          <img src="data:image/jpeg;base64,<%= brand.logo_img %>" alt="<%= brand.brand_name %> Logo" style="max-width: 100px;">
          <% } else { %>
          無LOGO
          <% } %> <br />
          商家視覺照1：
          <% if (brand.brand_img01) { %>
          <img src="data:image/jpeg;base64,<%= brand.brand_img01 %>" alt="<%= brand.brand_name %> Image 1" style="max-width: 100px;">
          <% } else { %>
          無圖片
          <% } %>
          商家視覺照2：
          <% if (brand.brand_img02) { %>
          <img src="data:image/jpeg;base64,<%= brand.brand_img02 %>" alt="<%= brand.brand_name %> Image 2" style="max-width: 100px;">
          <% } else { %>
          無圖片
          <% } %>
          商家視覺照3：
          <% if (brand.brand_img03) { %>
          <img src="data:image/jpeg;base64,<%= brand.brand_img03 %>" alt="<%= brand.brand_name %> Image 3" style="max-width: 100px;">
          <% } else { %>
          無圖片
          <% } %>
        </li>
        <% }) %>
      </ul>
    </main>
  </div>
  <footer>我的footer</footer>

</body>

</html>